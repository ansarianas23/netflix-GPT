// import OpenAI from 'openai';

// const openai = new OpenAI({
//   apiKey: import.meta.env.VITE_OPENAI_KEY,
//   dangerouslyAllowBrowser: true   // this is to bypass security by default openai does not allow us to make an api call normaaly this process is called in backend for security purposes
// });

// export default openai;